local TargetItem = "ShockMine"
local TargetPart = "HitDetect"
local ToggleKey = "Vortex_Shock_Control"

if _G[ToggleKey] then
    _G[ToggleKey] = nil
    if _G.ShockConnections then
        for _, conn in pairs(_G.ShockConnections) do
            if conn then conn:Disconnect() end
        end
        _G.ShockConnections = nil
    end
    warn("ANTI-SHOCK MINE: >>> DESATIVADO <<<")
    return 
end

_G[ToggleKey] = true
_G.ShockConnections = {}
local currentID = tick()
_G.ShockCurrentID = currentID
warn("ANTI-SHOCK MINE: >>> ATIVADO <<<")

local function removeTouch(obj)
    if obj.Name == TargetItem then
        local hit = obj:FindFirstChild(TargetPart)
        if hit then
            for _, child in pairs(hit:GetChildren()) do
                if child:IsA("TouchTransmitter") or child.Name == "TouchInterest" then
                    child:Destroy()
                end
            end
        end
    end
end

local function getFolders()
    local f = {}
    for _, c in pairs(workspace:GetChildren()) do
        if c.Name == "Entities" and c:IsA("Folder") then table.insert(f, c) end
    end
    return f
end

for _, folder in pairs(getFolders()) do
    for _, child in pairs(folder:GetChildren()) do
        removeTouch(child)
    end
    local conn = folder.ChildAdded:Connect(function(child)
        if _G[ToggleKey] == true and _G.ShockCurrentID == currentID then
            task.wait(0.1)
            removeTouch(child)
        end
    end)
    table.insert(_G.ShockConnections, conn)
end

task.spawn(function()
    while _G[ToggleKey] == true and _G.ShockCurrentID == currentID do
        for _, folder in pairs(getFolders()) do
            for _, child in pairs(folder:GetChildren()) do
                removeTouch(child)
            end
        end
        task.wait(1)
    end
end)