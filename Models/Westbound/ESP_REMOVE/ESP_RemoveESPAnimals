shared.BodyESP_Enabled = false -- BLOQUEIA O LOOP
shared.BodyESP_Data = {}

for _, animal in ipairs(game:GetService("Workspace").Animals:GetChildren()) do
    local highlight = animal:FindFirstChild("BodyHighlight")
    if highlight then highlight:Destroy() end
end
print("ESP Body Desativado.")