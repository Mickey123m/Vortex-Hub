local Players = game:GetService("Players")

for _, player in ipairs(Players:GetPlayers()) do
    local character = player.Character
    if character then
        local esp = character:FindFirstChild("Player_ESP_Internal")
        if esp then
            esp:Destroy()
        end
    end
end

print("ESP de Players removido.")