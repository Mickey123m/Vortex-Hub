local Workspace = game:GetService("Workspace")
local targetName = "Battery"

local function removeESP(item)
    -- Procura a pasta interna que foi criada (Item_ESP_Internal)
    local espFolder = item:FindFirstChild("Item_ESP_Internal")
    if espFolder then
        espFolder:Destroy()
    end
end

-- Varredura em todo o Workspace para encontrar os itens e remover o ESP
for _, obj in ipairs(Workspace:GetDescendants()) do
    if obj.Name == targetName then
        removeESP(obj)
    end
end

print("ESP de " .. targetName .. " removido com sucesso.")